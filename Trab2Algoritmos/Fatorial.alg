algoritmo "Fatorial" //acredito que o código está certo, porém o programa não quer exibir o resultado.

var

fatorial : inteiro
i : inteiro

inicio

fatorial <- 1
escreval("Digite um valor: ")
leia(i)
enquanto (i > 0) faca
fatorial <- fatorial*(i)
i <- i - 1

fimenquanto

escreval('O fatorial de '+i+' é '+fatorial)

fimalgoritmo

